name: Node.js

on:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '16.x'
    - run: npm ci
    - run: npm test
    - run: npx eslint .
    - name: code coverage 
      uses:  paambaati/codeclimate-action@v2.5.7
      env:
          CC_TEST_REPORTER_ID = e5ec66e0642c8a01db9f0fee18d050f5ec0bdb705089078e69dbdba9e9dc943d
      with:
          coverageCommand: npm test -- --coverage
          debug: true